from aiogram.types import Message


async def display_of_all_products_handler(message: Message):
    # Здесь будет логика для отображения всех продуктов
    await message.answer("Вы запросили отображение всех продуктов. Эта функция пока не реализована.")